# TODO 58: 全コードベース精査 (2026-06-10) — 指摘事項リスト

**Status: OPEN — CRITICAL 4/4 + HIGH 14/14 全決着 (2026-07-09)。残 = MEDIUM M1-M20 / LOW L1-L11 のトリアージのみ**
**Date:** 2026-06-10
**Method:** 4 並列レビューエージェント (reader / event_builder+merger / operator+node_agent / common+recorder+monitor+config+data_sink)。CRITICAL 項目はメインセッションでコード裏取り済み。

## トリアージ結果 (2026-07-09、8項目を並列検証エージェントで現行 master と照合)

| ID | 結果 | 内容 |
|----|------|------|
| **C1** | ✅ **FIXED (2026-07-09)** | `ReaderMetrics.current_run` 追加、`on_start` で保存 → EOS 3箇所 (collector / `send_eos` / emulator) に配線。**E2E 実証**: ローカル emulator スタック run77 で Recorder が `Received EOS run_number=77` → EOS 駆動ファイルクローズが初めて発火 |
| **C2** | ✅ 既修正 | `16ef3b4` (2026-06-11、レビュー翌日) で `scan_end = total_size.min(total_words)` + warn! 昇格。AMax 側も同コミットで修正 |
| **C3** | 🟦 **受け入れ (won't-fix、2026-07-09 ユーザー判断)** | Stop テール喪失は統計的に無害 (定常データの末尾数秒切断、バイアスなし)。ドレインバリアは実装しない。**ただし silent 禁止**: Merger receiver/sender + Recorder receiver の破棄3箇所に `dropped_batches` 計上 + info! (1件目+1000件毎) を追加。**CLAUDE.md データ保全ルールに例外を明文化** |
| **C4** | 🟦 受け入れ (同上) | multi-source first-EOS での他ソーステールも C3 と同じ Stop テール例外でカバー。Recorder は state-change fallback でファイルを確実に閉じる。ELIADE 8-host 運用で問題が出たら再訪 |
| **H1** | ✅ FIXED (2026-07-09) | dig2 Stop 信号を one-shot try_send → 3s retry loop (dig1 と同型)。channel bounded(1000) なので消失は現実的だった |
| **H2** | ✅ FIXED (2026-07-09) | dig1 Start 信号も 3s retry 化。消失 = RolloverTracker 前ラン持ち越し = ラン全体のタイムスタンプ破壊 (run30 と同 class) |
| **H3** | 🟦 受け入れ | Stop ドレイン中の try_send 破棄は C3 の Stop テール例外でカバー (drained 件数ログは既存) |
| **H4** | 🟦 受け入れ | Ctrl-C 時 EOS がダミーソケット行き。Recorder は state-change fallback で閉じるので実害はEOS 駆動クローズが shutdown 経路で効かないのみ。異常系として受け入れ |
| **H5** | ✅ 既修正 | `16ef3b4` でテスト改名 (`opendpp_to_event_data_unpacks_four_lanes_on_non_zero_channel`) + assertion 更新済み。master テストは現在 green |
| **H6** | 🟦 **方針決定 (2026-07-09)** | 旧 (pre-v3) `.delila` の読み出しは **C++ `tools/delila2root/TDelila.hpp` が担当** (v2 後方互換実装済み)。Rust 側 (EB file source / replay) は v3 のみサポート。旧ファイルを Rust 経路に食わせる必要が生じたら C++ で v3 に変換してから |
| **M19** | ✅ FIXED (2026-07-09) | emulator が Stop (Running→非Running エッジ) で EOS を送るように。run_number も C1 で配線済み → **emulator ベース統合テストが EOS 経路を検証可能に** (`config/config_emulator_local_test.toml` 新設) |

## HIGH トリアージ第2弾 (2026-07-09 同日)

| ID | 結果 | 内容 |
|----|------|------|
| **H7** | ✅ FIXED | Recorder 書込/クローズ失敗で `AtomicStats.write_failed` をラッチ → `effective_state` が **Error を報告**(Reader の hw_state パターン)。status_details に "WRITE FAILED" 表示。Configure(NewRun)/Reset(CloseFile) でクリア。disk full が「warn 1行で黙って全部捨てる」→「UI に Error」に |
| **H11** | ✅ FIXED | `end_run` (find_one+update) と `add_error` の Mongo filter に `status:"running"` 追加。run number 再利用(リテイク)で完了済 doc を上書き破壊できなくなった。`update_stats`/`add_note` は既に filter 有り |
| **H12** | ✅ FIXED | stop handler の統計を全コンポーネント合算(~4倍)→ **Recorder authoritative** に(get_status と同パターン)。MongoDB/ELOG に入る値が正確に |
| **H13** | ✅ FIXED | `/api/reset` が active run を **`RunStatus::Aborted` で end_run + current_run クリア**してから reset_all。孤児 "running" doc と current_run 残留を解消。reset 自体の state ガードは意図的に付けない(escape hatch = UI "Force Reset") |
| **H14** | ✅ FIXED | `AppState.run_control_lock: tokio::sync::Mutex<()>` で configure/arm/start/stop/reset/run_start を直列化(tuneup_apply_lock と同パターン)。TOCTOU 消滅。E2E で configure→…→reset 全サイクルがロック経由で正常動作確認 |
| **H8** | ✅ FIXED (2026-07-09) | **drain ループを撤去**が正解と判明: stale batch は `write_raw_batch` の `run_active=false` ガードで既に no-op、一方 try_recv drain は DrainAndStart より**後ろ**のキュー(=新ランの先頭バッチになり得る)を食っていた。ドレイン撤去で競合ごと消滅 |
| **H9** | ✅ FIXED (2026-07-09) | `PipelineStats.write_failures` を新設し writer thread に配線。ROOT 書込失敗を計上 + `discarded_events`/`total_failures` 付き error!、CLI サマリにも "Write FAILURES" 表示。buffer 破棄自体は受け入れ(派生データ、raw は Recorder が保持)だが silent でなくなった |
| **H10** | ✅ FIXED (2026-07-09) | **チャンク境界のコインシデンス欠落を修正**: ① `sort_and_split` の retain を `core_end - lookback`(coincidence+multiplicity 窓の最大、pipeline が TriggerConfig から算出)からに ② `SortedChunk.core_start`(emit floor = 前チャンクの core_end)を新設し、lookback で持ち越されたトリガーの**二重 emit を防止**。回帰テスト3本(イベント**内容**を検証): backward-window partner 保持 / 二重 emit なし / **boundary 跨ぎ multiplicity 発火**(旧コードではコインシデンス丸ごと消失)。ELIADE clover addback の前提整備完了 |

**残り (未トリアージ):** MEDIUM M1-M18/M20、LOW L1-L11。

## 総括

最も深刻なのは個別バグではなく、**「絶対にデータを落とさない」ルールが Stop/EOS フロー全体で守られていない**こと。EOS の run_number が常に 0、Merger/Recorder が非 Running 時のデータを黙って捨てる、stop_all にドレインバリアがない——この 3 つが組み合わさり、**全ての通常 Stop でパイプライン内のテールデータが失われ得る**構造になっている。加えてデコーダに無限ループ 2 件、master でテスト 1 件失敗。

## 推奨着手順

1. **EOS run_number=0 修正**(数行、効果大)+ master のテスト失敗修正 — 即日級
2. **デコーダ無限ループ 2 件** — `decode_event` が `None` を返す前に `word_index = total_size` で脱出、`validate_header` の mismatch を `warn!` 昇格
3. **Stop フロー設計**: per-source EOS カウント + stop_all のドレインバリア + Merger/Recorder の非 Running 破棄廃止 — Stop セマンティクス (EOS 待ち vs タイムアウト) の設計判断が必要
4. **MessagePack 互換性** — 既存 `.delila` アーカイブ資産の読み出し可否に直結。実旧ファイルでの検証を

---

## 🔴 CRITICAL

### C1. EOS の run_number が全箇所ハードコード 0 → Recorder の stale-EOS フィルタが本物の EOS を全部捨てる
- `src/reader/mod.rs:1617`, `src/reader/mod.rs:2568`, `src/reader/mod.rs:2636`(エミュレータも `src/data_source_emulator/mod.rs:609`)
- Cross-Run EOS Fix で導入した run_number フィールドに Reader が現在の run 番号を渡していない。Recorder は run_number 不一致の EOS を stale として無視 (`src/recorder/mod.rs:895-901`) → run_number ≥ 1 の全ランで EOS 駆動のファイルクローズが一度も発火せず、state-change 経路のフォールバック頼み。
- **裏取り済み** (grep で 3+1 箇所確認)

### C2. PSD2/PHA2 デコーダ無限ループ — ヘッダの total_size > 実データ長で DecodeLoop ハング
- `src/reader/decoder/dualchannel_common.rs:334` + `:392-396`
- `decode_event` は `word_index >= total_words` で **word_index を進めずに** `None` を返すが、外側ループは `while word_index < total_size`(ヘッダ値)。`validate_header` はサイズ不一致を `debug!` で許容して通す (`:574-582`、これ自体 silent failure ルール違反) → truncated read 1 回で worker スレッド永久スピン → Reader 全体フリーズ。
- 同型バグ: AMax Raw デコーダ `src/reader/decoder/amax.rs:212-234`(端数 1 ワードでスピン。オフライン replay パスのみ)
- **裏取り済み**

### C3. Stop シーケンスにドレインバリアがない — 全コンポーネントが非 Running データを無言破棄
- Merger: `if !is_running { continue; }` で receiver/sender とも破棄、ログもカウンタもなし (`src/merger/mod.rs:444-446`, `:538-539`) — **裏取り済み**
- Operator `stop_all` は上流からコマンド ACK だけ見て即次へ (`src/operator/client.rs:384`) — データドレイン完了を待たない
- Recorder receiver も非 Running 着信を破棄
- HWM=0 で ZMQ バッファに数秒分溜まり得る → **Stop のたびにラン末尾のデータが落ちる**構造。「落とすくらいなら止める」ルールの真逆。

### C4. マルチソース時、最初の EOS でファイル/パイプラインを閉じる (per-source EOS カウントなし)
- Recorder: 最初に run_number 一致した EOS で `end_run()`、以降の他ソースのバッチは `debug!` で破棄 (`src/recorder/mod.rs:891-921`)
- EB `ZmqHitSource`: first-EOS latch + run_number フィルタなし (`src/event_builder/source.rs:587-624`) — 前ランの遅延 EOS が新ランを数 ms で殺すシナリオも成立
- ZMQ fair-queuing はソース間順序を保証しない → ソース A の EOS 処理時にソース B の最終バッチがキュー内、は普通に起きる。Merger が N 個の EOS をそのまま転送する以上、下流は **N 個カウントしてから閉じる**必要がある。

---

## 🟠 HIGH

### Reader / Stop-Start フロー
- **H1. AMax/DIG2 の Stop 信号が `let _ = try_send` 一発** (`src/reader/read_loop_dig2.rs:254`) — channel full なら EOS 消失。MEMORY.md に「3s retry loop 化済み」とあるので**リグレッション**。
- **H2. DIG1 の Start 信号も一発 try_send** (`src/reader/read_loop_dig1.rs:200`) — 落ちると `reset_for_new_run()` 不実行 → RolloverTracker が前ラン状態を持ち越し → **ラン全体のタイムスタンプ破壊**(過去の「Run 間リセット不整合」と同 failure class)。sequence_number もリセットされない。
- **H3. Stop ドレイン中のイベントを無言で `let _ = try_send`** (`src/reader/read_loop_dig1.rs:216`, `src/reader/read_loop_dig2.rs:246`) — full なら破棄、ログゼロ。dig2 側はドレインに件数/時間上限もない。
- **H4. シャットダウン時の EOS がダミーソケット行き** (`src/reader/mod.rs:2795-2845`) — `mem::replace` で実ソケットを decode_loop に移した後、`send_eos()` が `tcp://127.0.0.1:0` に publish。さらに ReadLoop の shutdown パスは Stop 処理ブロックの前に `break` → **Running 中の Ctrl-C ではどの経路でも EOS が出ない**。
- **H5. `cargo test` が master で失敗**: `opendpp_to_event_data_no_unpack_on_non_zero_channel` (`src/reader/mod.rs:3137`) — commit `37ce069` で ch0 ゲートを外したのにテスト未更新(実行確認済み: `left: [1000], right: [1000, 2000, 3000, 4000]`)。doc コメントも旧動作のまま。

### Recorder / 共通
- **H6. MessagePack 後方互換が壊れている** (`src/common/mod.rs:126-216`, `:258-281`) — rmp-serde は positional array なので `#[serde(default)]` は**末尾**欠損しか救えない。`EventData.user_info`(`waveform` の前に挿入)、`Waveform.analog_probe3`/`digital_probe5..16`(途中挿入)により、**波形入りの旧 `.delila` は現行バイナリで読めない**(`DataBlockIterator` がファイル全体 abort、`src/recorder/format.rs:745-751`)。コメントの互換性主張は誤り。フォーマットバージョン bump + 旧実ファイルでの互換テストが必要。波形なし旧ファイルはたまたま読める(純粋な末尾欠損)。
- **H7. Recorder 書き込み失敗が warn のみで Running 継続** (`src/recorder/mod.rs:886-889`) — disk full でラン残り全部を黙々と捨てる。Error 状態遷移すべき。ファイルクローズ失敗 (`:904`, `:941`, `:945`) も同様。
- **H8. `DrainAndStart` のドレインが新ランの先頭バッチを巻き込む競合** (`src/recorder/mod.rs:915-936`) — drain は処理時点の try_recv ループなので、watch が Running に切り替わった後に届いた新ランのバッチを stale 扱いで破棄し得る。境界センチネルで止めるべき。
- **H9. EB の ROOT 書き込み失敗で `buffer.clear()`** (`src/event_builder/pipeline.rs:670-674`) — disk full で 100k built events 喪失、Operator へのエラー伝搬なし、以降のバッチも毎回 clear。

### Event Builder ロジック
- **H10. チャンク境界で backward-window のヒット欠落、境界跨ぎ multiplicity トリガは丸ごと消失** (`src/event_builder/chunk_builder.rs:307-308` vs `:170-201`) — `sort_and_split` は `ts >= core_end` しか retain しないが、`[core_end, core_end+window)` のトリガの窓は core_end より前に届く。retain を `core_end - max(window, multiplicity windows)` からにする必要。既存テストはイベント**数**しか見ないので検出不能。

### Operator
- **H11. run_number 再利用(リテイク)時、`end_run` が古い MongoDB ドキュメントを上書き** (`src/operator/run_repository.rs:204-234`) — `status: "running"` フィルタ欠如。古い完了 run の end_time/duration が破壊され、新 run doc は永久に "running"。`add_error` (`:280`) も同罪。
- **H12. stop 時の run 統計が全コンポーネント合算で約 4 倍水増し** (`src/operator/routes/status.rs:379-395`) — Reader+Merger+Recorder+Monitor が同じイベントを数えたものを MongoDB と ELOG に永続化。`get_status` 同様 Recorder を authoritative にすべき。
- **H13. `/api/reset` が Running 中でも無ガードで全停止** (`src/operator/routes/status.rs:464-476`) — run record も孤児化 (`end_run` 不呼出、`current_run` 残留)。`run_start` の Running ガードも `Mixed` 状態をすり抜ける (`:518`)。
- **H14. run 制御の直列化なし — 全ガードが TOCTOU** (`src/operator/routes/status.rs:501-530`, `src/operator/routes/tuneup.rs:43-119`) — 並行 `run_start` 2 本が両方ガードを通過し Reset/Configure/Arm/Start が交錯。マルチクライアントは明示機能 (`next_run_number`) なので現実に踏み得る。run 制御全体を 1 つの tokio Mutex で直列化するのが最小修正。

---

## 🟡 MEDIUM

- **M1.** config ロード失敗でも `hw_configured=true` (`src/reader/read_loop_dig1.rs:151-155`, `read_loop_dig2.rs:175-178`) — 直前の `/cmd/reset` で工場出荷設定に戻っているのに「現設定維持」コメントで Arm/Start 続行可能。`auto_config_failed` を立てるべき。
- **M2.** Arm/Start ハードウェアコマンド失敗でも `hw_armed`/`hw_running` が true に (`read_loop_dig1.rs:181-195`, `read_loop_dig2.rs:205-219`) — データゼロの「Running」をオペレータに見せる。
- **M3.** CAEN エラーコードの `transmute` が UB (`src/reader/caen/error.rs:40`) — rustified enum へ未定義 i32 を transmute。将来の FELib 新エラーコードで即 UB。match に置換を。
- **M4.** PSD2/PHA2 で `num_channels: 32` ハードコード (`src/reader/mod.rs:2357`, `:2383`) — VX2745 (64ch) で上位 32ch がメトリクス欠落 + warn 洪水 (`per_channel_counts` も `MAX_CHANNELS=32`)。
- **M5.** `reconstruct_subcounter` の u64 アンダーフロー (`src/reader/decoder/rollover.rs:166-168`) — ラン開始 ~4.3 s 間 (extended < 2^31) に candidate > extended だと wrapping_sub で ~1.8e19 → 10^19 ns 級タイムスタンプ。proptest 契約 (`r <= extended`) と単体テスト期待値が矛盾している点も要確認。
- **M6.** config に unknown-key 検出なし — TOML/JSON タイポが無言でデフォルト値に。legacy キーがあるので `deny_unknown_fields` 一律は不可、ロード時の unknown-key 警告パスを。
- **M7.** `num_channels` の serde デフォルト 32 が PSD1 実機 (8/16ch) と乖離 (`src/config/digitizer.rs:495-497` vs `:1405-1410`) — VX1730B=16ch の前科と同根。FW capabilities 由来 or 必須化を。
- **M8.** SIGTERM 非対応 (`src/common/shutdown.rs:32-59`) — SIGINT のみ。systemd stop / kill で BufWriter 最大 64KB + footer 消失。`SignalKind::terminate()` arm 追加を。
- **M9.** command_task が一時エラーで永久終了 (`src/common/command_task.rs:119-155`) — 以降そのコンポーネントは制御不能 (kill しか手がない)。re-bind/retry に。
- **M10.** Monitor `/api/status` が GetSnapshot で全ヒストグラム deep-clone (`src/monitor/mod.rs:961-985`) — 16-32 MB/call、1 Hz ポーリングでホットパス圧迫。`GetListSummary` で足りる。
- **M11.** Recorder writer がフッタの timestamp 範囲のためだけに全バッチ再デシリアライズ(波形込み) (`src/recorder/mod.rs:417-432`) — 高レートで writer ボトルネック → unbounded channel の RSS 成長。positional scan か上流からタイムスタンプ携行で解消。
- **M12.** node_agent の stop が即 SIGKILL (`src/node_agent/process.rs:341-344`) — flush なしで Recorder/Reader を殺す。SIGTERM → timeout → SIGKILL に。Restarting 状態のプロセスを API から止められない (`:127-144`) 件も同根。
- **M13.** `/api/configure` が ApplyDigitizerConfig の `resp.success` を見ない (`src/operator/routes/status.rs:137-165`) — FW 拒否が完全無音 + HTTP 200。run_start Phase 1.5 も warn のみで Arm/Start 続行 + DashMap に config 無い digitizer を無言 skip (`:599-649`)。
- **M14.** `tuneup_apply` が path id と active tuneup digitizer の一致を確認しない (`src/operator/routes/tuneup.rs:328-404`) — 別 id に apply すると idle digitizer の config/JSON を上書きし、live tuneup の Merger/Monitor を巻き込んで停止。hardware apply 前に disk/memory 永続化する順序も問題。
- **M15.** `/api/stop`/`/api/configure`/`/api/arm` に Tune Up ガードなし (`src/operator/routes/status.rs:362-453`, `:119-196`) — tuneup 中の stop で tuneup フラグが残留し 409 デッドロック。
- **M16.** MongoDB 書き込み失敗 (end_run/create_run_snapshot) が warn のみで HTTP 200 (`src/operator/routes/status.rs:409-419`, `:317-321`, `:747-751`) — run history の欠損がオペレータに見えない。
- **M17.** `/api/start` の in-memory `current_run` フォールバックが `digitizer_repo` の if にぶら下がっている (`src/operator/routes/status.rs:309-343`) — `run_start` (`:703-737`) が正しい構造、start はコピーミス。
- **M18.** Merger の sequence gap / source restart 検出がログされない (`src/merger/mod.rs:81-102`, `:454-457`) — `restarted` bool を call site が無視。検出時に `warn!` を。
- **M19.** エミュレータが Stop で EOS を送らない + shutdown EOS も run_number=0 (`src/data_source_emulator/mod.rs:608-611`, `:756-759`) — **エミュレータベースの統合テストは本番 EOS クローズパスを一切検証していない**。
- **M20.** EB Stop tear-down が SUB ソケット内の未処理ヒットを破棄 (`src/bin/online_event_builder.rs:400-449` + `source.rs:590-594`) + watch channel の値 coalescing で高速 Stop→Start 時に旧パイプライン残留の可能性。

---

## 🟢 LOW

- **L1.** 本番パスの `.unwrap()`/`.expect()` 残存: hw_state ロック (read_loop 両方)、RateTracker mutex (`src/reader/mod.rs:879/904`, recorder, emulator)、MongoDB `to_bson` (`run_repository.rs:229/230/258/285`)、EB pub スレッド spawn (`pipeline.rs:199`)、`mod.rs:1743/2801` 等。
- **L2.** Recorder `dropped_batches` カウンタがどこからもインクリメントされず常に 0 表示 (`src/recorder/mod.rs:100,111`) — 偽の安心感。
- **L3.** Histogram2D のエッジ値が overflow でなく隣の行 column 0 に誤ビン (`src/monitor/mod.rs:236-249`) — f32 丸めで `x_bin == x_bins` のケース。Histogram1D は正しく overflow 行き。
- **L4.** EB の final flush chunk 送信が `let _ =` (`pipeline.rs:315-331`) — worker 早期終了時にソーター残量全部が無言消失。
- **L5.** EB `batch_id` が per-worker 競合で順序保証なし (`pipeline.rs:407-417`) — gap 検出用フィールドとして信頼できない。
- **L6.** TimeCalibration の malformed key 無言 skip (`src/event_builder/config.rs:169-177`)。
- **L7.** `(1u32 << num_channels) - 1` が num_channels=32 で overflow (`src/reader/caen_legacy/handle.rs:763`)。
- **L8.** `CaenHandle` の「NOT Send/Sync」コメントが嘘 (`src/reader/caen/handle.rs:1718-1719`) — u64 のみで auto-derive。型システムで FELib 単一スレッド制約を強制していない。
- **L9.** stop 後に最終統計を取得 + 部分失敗でも `RunStatus::Completed` 記録 (`src/operator/routes/status.rs:366-379`)。
- **L10.** Stop/シャットダウン時バックプレッシャーでの意図的ドロップ (warn 付き) が複数 (`read_loop_dig1.rs:401-407`, `read_loop_dig2.rs:485-491`, dig1 ドレイン上限 1000 件/1 s + CLEAR_DATA) — 「stop > data」の方針なら CLAUDE.md に明文化を。
- **L11.** Monitor の bounded(1000) 意図的ドロップ (`src/monitor/mod.rs:1114-1116`) — display-only として既知・カウント済みだが、CLAUDE.md ルールの明示的例外として書面化推奨。

---

## ✅ 確認済みクリーン項目

- ZMQ HWM=0 ポリシー: 全コンポーネント遵守 (`pub_no_hwm`/`sub_no_hwm` 経由、reader/merger/recorder/monitor/data_sink/emulator/EB)
- Merger の zero-copy 転送、`MultiplicityTrigger::compute_anchors`(チャンク内)、time_offsets DFS resolver、L2 評価順序、Recorder ファイル名衝突回避 + footer/checksum recovery
- reader 単体テスト 215/216 pass (失敗 1 件は H5)
